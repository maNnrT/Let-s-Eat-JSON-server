{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "api/server.js",
  "scripts": {
    "dev": "SET NODE_ENV=development & node api/server.js",
    "start": "SET NODE_ENV=production & node api/server.js"
  },
  "dependencies": {
    "clone": "^2.1.2",
    "json-server": "^0.17.0"
  }
}
